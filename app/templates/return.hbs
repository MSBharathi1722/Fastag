{{#unless this.content}}
  <div class="returnBox1">
    <h2>Your Last few Travel Details</h2>
    {{#each this.model as |list|}}
      <div class="content">
        <div><h3>{{list.place}}&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;{{list.time}}&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;<button type="button" {{on 'click' (fn this.pay list.id)}}>Pay</button></h3></div>
      </div>
    {{/each}}
  </div>
{{/unless}}
{{#if this.selected}}

{{/if}}
{{#if this.receipt}}
<div class="receipt-body">
          <div class="card">
              {{!-- template-lint-disable no-inline-styles --}}
              <div style="border-radius:200px; height:200px; width:200px; background: #F8FAF5; margin:0 auto;">
                <i class="checkmark">✓</i>
              </div>
                <h1 id="h1">Success</h1>

                {{!-- template-lint-disable no-action --}}
                <p id="p">Free Return Available<br/> Place : {{this.place}} Toll Plaza<br>THANK YOU<br><button type="button" {{action this.reload}} ><LinkTo @route="home">Go to home?</LinkTo></button><br> </p>
          </div>
        </div>
{{/if}}
{{outlet}}
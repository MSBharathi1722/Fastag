{{#unless this.content}}
  <div class="returnBox1">
    <h2>Check For a valid Return</h2>
    <form>
      <label>Toll place:</label><br>
      <Input
          class="input"
          aria-label="Toll Place"
          @type="text"
          placeholder=""
          @value={{this.place}}
      /><br><br>
        {{!-- template-lint-disable no-action --}}
      <button id="bt" type="button" {{action this.validate}}>Proceed</button>
      <p>You can avail your free return if you have crossed this poll within 30 minutes<br>Else do a fresh payment for toll</p>
    </form>
  </div>

{{/unless}}
{{#if this.receipt}}
<div class="body">
          <div class="card">
              {{!-- template-lint-disable no-inline-styles --}}
              <div style="border-radius:200px; height:200px; width:200px; background: #F8FAF5; margin:0 auto;">
                <i class="checkmark">✓</i>
              </div>
                <h1 id="h1">Success</h1>

                {{!-- template-lint-disable no-action --}}
                <p id="p">Free Return Available<br/> Place : {{this.place}} Toll Plaza<br>THANK YOU<br><button type="button" {{action this.reload}} ><LinkTo @route="home">Go to home?</LinkTo></button><br> </p>
          </div>
        </div>
{{/if}}
{{outlet}}